mkdir dbt_dev
cd dbt_dev
--  création de l'env venv
python -m venv dbt_dev
--activer l'env
.\dbt-env\Scripts\activate
-- editer le profiles.yml

-- installation dbt_devpip install dbt-sqlserver
pip install dbt-core dbt-sqlserver
- check la version
dbt --version
--lancer l'initialisation
dbt initialisation
-- tester
dbt debug 

-- profiles.yml
football_project:
  target: dev
  outputs:
    dev:
      type: sqlserver
      driver: 'ODBC Driver 17 for SQL Server' # Vérifie ta version installée (17 ou 18)
      host: localhost\SQLEXPRESS
      port: 1433
      user: sa
      password: "****"
      database: FOOTBALL
      schema: CHAMPIONS
      threads: 1
      trust_cert: True # Recommandé pour les instances locales Express